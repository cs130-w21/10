import React from 'react';
// import {useAuth} from '../../services/AuthContext'

/* @jordan, replace this with whatever you want for the ProfileCompletion page :) 
    also now that i think about it, plz replace it w/ a component called CompleteProfile
    or something like that, not Register lmao.
*/
export default function Register() {
    return (
        <div>
            <h2>Register</h2>
            {/* <p>{uid ? uid : 'no uid'}</p>
            <p>{userData ? JSON.stringify(userData) : 'no user data'}</p>
            <button onClick={() => handleLogout()}>Sign Out</button> */}
        </div>
    );
}
